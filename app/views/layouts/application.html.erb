<!DOCTYPE html>
<html>
  <head>
    <title>My book blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <!-- make the design responsive for phone -->
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>

    <meta charset="UTF-8">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/js/all.min.js"></script>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Overpass+Mono:wght@300..700&family=Playwrite+ES+Deco:wght@100..400&display=swap" rel="stylesheet">

    <style>
         
      html {
        min-height:100%;
        padding: 0px 20px;
      }
      
      body{
        padding-top: 20px;
        padding-bottom: 20px;
        background: linear-gradient(360deg, #e8e8e4, #ebeae4, #edece4, #f0eee4, #f4f0e4, #f7f2e4, #fbf3e4, #fff5e4);
        min-height:100%;
        color: #565655;
        font-family: "Overpass Mono", monospace;
        font-optical-sizing: auto;
        font-weight: 300;
        font-size: 15px;    
      }

      .avatar-row {
        padding: 50px 50px;
        margin: auto;
        text-align: center;
      }

      .avatar-image {
        text-align: center;
        width: 138px;
        height: 138px;
        object-fit: cover; /* prevent the image from being squished */
        border-radius: 128px; /* make it round  */    
        margin: 0;
        padding: 0;    
      }

      .name-row {
        margin: 0;
        padding: 0;  
        text-align:center;
        font-family: "Playwrite ES Deco", cursive;
        font-size: 26px;
        font-weight: 900;
        font-optical-sizing: auto;
      }

      .blog-name {
        margin-top: 50px;
        margin-bottom: 50px;
        text-align:center;
        font-family: "Playwrite ES Deco", cursive;
        font-size: 50px;
        font-weight: 900;
        font-optical-sizing: auto;
        color: #edece4;
      }
      .footer 
      {
        padding: 10px;
        color: #edece4;
        position: absolute;
        bottom: 0;
      }

    </style>
  </head>

  <body>
    <nav>
    <a href="/">Home</a>
    <% if current_user != nil %>
      <a href="/users/<%= current_user.id %>">My blog</a>
      <a href="/users/edit">Change password</a>
      <a href="/users/sign_out">Sign out</a>
    <% else %>
      <a href="/users/sign_up">Sign up</a>
      <a href="/users/sign_in">Sign in</a>
    <% end %>

<!--    <a href="/users">Users</a>
    <a href="/blogs">Blogs</a>-->
    </nav>
    
<!--    <p class="notice"><%#= notice %></p>
   	<p class="alert"><%#= alert %></p>-->

    <% if notice.present? %>
      <div style="color: green">
        <%= notice %>
      </div>
    <% end %>


    <% if alert.present? %>
      <div style="color: red">
        <%= alert %>
      </div>
    <% end %>


    <%= yield %>
  </body>
</html>
